import { ModuleHeaderPipe } from './module-header.pipe';

describe('ModuleHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new ModuleHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
