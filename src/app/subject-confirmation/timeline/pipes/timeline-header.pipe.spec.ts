import { TimelineHeaderPipe } from './timeline-header.pipe';

describe('TimelineHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new TimelineHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
